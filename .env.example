# AI provider — set to "mock" for local dev (no API key needed)
# Options: mock | openai | claude
AI_MODE=mock

# OpenAI (only needed when AI_MODE=openai)
OPENAI_API_KEY=sk-...

# Anthropic / Claude (only needed when AI_MODE=claude)
ANTHROPIC_API_KEY=sk-ant-...
